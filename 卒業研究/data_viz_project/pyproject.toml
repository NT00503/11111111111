[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "your_package_name"
version = "0.1"
dependencies = [
    "pandas",
    "matplotlib",
    "requests",
    "openai"
]
